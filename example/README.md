# Example

The example demonstrates package construction, listening for incoming App Links,
building a canonical link, and using a callback-backed short-link provider.

Create platform folders with `flutter create .` inside this directory before
running it, then configure Android App Links and iOS Universal Links for your
own domain.
